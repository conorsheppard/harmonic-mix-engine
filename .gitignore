*.class
*.iml
# Ignore Gradle project-specific cache directory
.gradle

# Ignore Gradle build output directory
build

# Node / Next.js
node_modules
.next
out
.env*.local

classpath.txt

# Terraform (keep .terraform.lock.hcl committed for reproducible provider versions)
terraform/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# SSH private keys (retrieved from Terraform state locally)
.priv/*.pem